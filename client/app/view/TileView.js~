Ext.define('moneyworld.view.MainTileView', {
	extend: 'Ext.dataview.DataView',
	xtype: 'maintileview',
	requires: [
	],
	config: {
		styleHtmlContent: true,

		layout: {
			type: 'fit'
		}

		inline {
			wrap: true
		}

		itemCls: 'tileview-item',

		itemTpl: '<div><h1>{indicator}</h1><div class="tileview-item-{indicator}"></div><span>{value}</span></div>',

		itemConfig: {
			action: 'detailedview'
		}

		store: 'mainstore'
	}
});
